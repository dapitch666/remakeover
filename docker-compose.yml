version: '3.8'

services:
  remakeover:
    build: .
    container_name: remakeover
    ports:
      - "8501:8501"
    volumes:
      # Persists configuration and data across container restarts
      - ./data:/app/data
    restart: unless-stopped
